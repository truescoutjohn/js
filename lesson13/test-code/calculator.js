export const getSquaredArray = numbers => numbers.map(number => number ** 2);

export const getOddNumbers = numbers => numbers.filter(number => number % 2);

export default (number1, number2) => number1 + number2;
